services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lms-server
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 5000
    restart: unless-stopped
